---
phase: 02-typed-mission-graph-todo-write-through-and-handoff-authority
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hermes_state.py
  - agent/mission_state.py
  - tools/mission_tool.py
  - tests/test_hermes_state.py
  - tests/agent/test_mission_state.py
  - tests/tools/test_mission_tool.py
autonomous: true
requirements:
  - MISSION-01
user_setup: []
must_haves:
  truths:
    - Operators and downstream surfaces can create, approve, attach, and read missions from canonical state without replaying runtime-signal audit history.
    - Mission graph records survive database reopen and session continuation as first-class Hermes state.
    - Mission activation and attachment remain explicit operations rather than implicit side effects of observational events.
  artifacts:
    - hermes_state.py
    - agent/mission_state.py
    - tools/mission_tool.py
    - tests/test_hermes_state.py
    - tests/agent/test_mission_state.py
    - tests/tools/test_mission_tool.py
  key_links:
    - SessionDB migrations and query helpers in hermes_state.py back the mission service in agent/mission_state.py.
    - tools/mission_tool.py delegates all writes and reads to the mission service instead of duplicating SQL or reconstructing mission state from audit history.
    - Migration and lifecycle tests prove old sessions still open while new mission tables and APIs behave canonically.
---

<objective>
Stand up the canonical mission graph foundation for Phase 2: schema, migration coverage, lifecycle APIs, and the first read/write service boundary.

Purpose: Everything else in this phase depends on a truthful mission authority inside Hermes state. This plan establishes that authority before any todo, handoff, or checkpoint adapter work lands.
Output: Versioned mission tables in `state.db`, a dedicated mission service module, a mission tool/read surface, and focused migration/lifecycle regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-typed-mission-graph-todo-write-through-and-handoff-authority/02-CONTEXT.md
@.planning/phases/02-typed-mission-graph-todo-write-through-and-handoff-authority/02-RESEARCH.md
@.omx/plans/prd-hermes-all-features-integration.md
@.omx/plans/test-spec-hermes-all-features-integration.md
@hermes_state.py
@tools/todo_tool.py
@run_agent.py
</context>

<tasks>

<task type="auto">
  <name>task 1: add canonical mission graph tables and migration coverage</name>
  <files>hermes_state.py, tests/test_hermes_state.py</files>
  <action>Extend `SessionDB` with versioned schema changes for `missions`, `mission_nodes`, and `mission_links`, plus any mission/session attachment metadata needed for explicit lifecycle operations. Reuse existing WAL/migration/index helpers instead of introducing a second store. Keep `runtime_signal_audit` observational only; do not infer canonical mission state from audit rows. Add migration tests that prove an existing database upgrades cleanly and that mission CRUD/lifecycle transitions survive reopen.</action>
  <verify>
    <automated>scripts/run_tests.sh tests/test_hermes_state.py -q</automated>
  </verify>
  <done>`tests/test_hermes_state.py` proves schema migration, CRUD, lifecycle transitions, and reopen behavior for canonical mission records without corrupting pre-existing session data.</done>
</task>

<task type="auto">
  <name>task 2: implement the mission service and mission tool boundary</name>
  <files>agent/mission_state.py, tools/mission_tool.py, hermes_state.py</files>
  <action>Create `agent/mission_state.py` as the single service layer for mission lifecycle, node/link mutation, mission attachment/detachment, and read queries. Add `tools/mission_tool.py` as the adapter surface for these operations. Keep the API typed and explicit: activation is approval-gated, attachment is session-scoped but mission state is cross-session, and read queries return canonical mission graph data for later CLI/TUI/web consumers. Do not build dashboard UX and do not create a parallel command dispatcher or writable projection.</action>
  <verify>
    <automated>scripts/run_tests.sh tests/agent/test_mission_state.py tests/tools/test_mission_tool.py -q</automated>
  </verify>
  <done>The mission tool delegates to `agent/mission_state.py`; no mission SQL is duplicated in tool handlers, and lifecycle/read behaviors are exercised by focused tests.</done>
</task>

<task type="auto">
  <name>task 3: lock in the Phase 2 service contract with focused regressions</name>
  <files>tests/test_hermes_state.py, tests/agent/test_mission_state.py, tests/tools/test_mission_tool.py</files>
  <action>Add coverage for the invariants the rest of Phase 2 will depend on: explicit mission creation/approval/attach flows, parent-child node relationships, typed mission links, read APIs that return canonical graph state without consulting runtime-signal audit history, and a negative regression that proves unauthorized mission activation is rejected. Keep tests narrow and deterministic so later plans can depend on this contract confidently.</action>
  <verify>
    <automated>scripts/run_tests.sh tests/test_hermes_state.py tests/agent/test_mission_state.py tests/tools/test_mission_tool.py -q</automated>
  </verify>
  <done>The mission graph contract is test-defined and reusable: downstream plans can assume canonical mission persistence and typed read APIs exist, and unauthorized activation is rejected by focused regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] scripts/run_tests.sh tests/test_hermes_state.py tests/agent/test_mission_state.py tests/tools/test_mission_tool.py -q
- [ ] Existing non-mission session records still load after the schema migration under test.
- [ ] Mission reads and lifecycle actions are served by the mission service/tool boundary, not by replaying audit rows or scanning old tool outputs, and focused regressions reject unauthorized activation.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Hermes has one canonical mission graph authority in `state.db`, with explicit service APIs and migration coverage that later Phase 2 plans can depend on.
</success_criteria>

<output>
After completion, create `.planning/phases/02-typed-mission-graph-todo-write-through-and-handoff-authority/02-01-SUMMARY.md`
</output>
