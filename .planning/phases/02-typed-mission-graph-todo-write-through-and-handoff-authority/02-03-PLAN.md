---
phase: 02-typed-mission-graph-todo-write-through-and-handoff-authority
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - agent/handoff_packets.py
  - agent/mission_state.py
  - agent/skill_commands.py
  - tools/delegate_tool.py
  - tools/checkpoint_manager.py
  - tools/mission_tool.py
  - tests/tools/test_delegate.py
  - tests/tools/test_checkpoint_manager.py
  - tests/agent/test_handoff_packets.py
  - tests/agent/test_skill_commands.py
autonomous: true
requirements:
  - MISSION-03
user_setup: []
must_haves:
  truths:
    - Delegated work produces typed, mission-linked handoff records that later sessions and surfaces can query without replaying prompt history.
    - Mission/workflow checkpoints are readable from canonical mission state while filesystem rollback checkpoints remain a separate safety mechanism.
    - Existing Hermes command/tool surfaces can read mission, handoff, checkpoint, and projection-only bundle state without introducing a second runtime or writable mirror.
  artifacts:
    - agent/handoff_packets.py
    - agent/mission_state.py
    - tools/delegate_tool.py
    - tools/checkpoint_manager.py
    - tools/mission_tool.py
    - agent/skill_commands.py
    - tests/tools/test_delegate.py
    - tests/tools/test_checkpoint_manager.py
    - tests/agent/test_handoff_packets.py
    - tests/agent/test_skill_commands.py
  key_links:
    - tools/delegate_tool.py writes typed handoff packets through agent/handoff_packets.py and stores authoritative mission relationships via agent/mission_state.py.
    - tools/checkpoint_manager.py stays limited to filesystem rollback while mission checkpoints persist separately through canonical mission APIs.
    - tools/mission_tool.py and any narrow command exposure in agent/skill_commands.py read the same canonical mission/handoff/checkpoint state consumed later by CLI, TUI, and web surfaces.
---

<objective>
Finish Phase 2's workflow authority layer by persisting mission-linked handoff packets, mission checkpoints, and projection-only bundle/read models without conflating them with filesystem rollback or audit streams.

Purpose: After the mission graph and todo write-through semantics exist, Hermes still needs durable workflow handoffs and checkpoints that later Mission Control surfaces can trust. This plan lands that authority without creating a second state path.
Output: Typed handoff packet persistence, mission checkpoint records, projection-only bundle/read queries, minimal command exposure through existing Hermes helpers, and regressions that protect checkpoint separation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-typed-mission-graph-todo-write-through-and-handoff-authority/02-CONTEXT.md
@.planning/phases/02-typed-mission-graph-todo-write-through-and-handoff-authority/02-RESEARCH.md
@.omx/plans/prd-hermes-all-features-integration.md
@.omx/plans/test-spec-hermes-all-features-integration.md
@.planning/phases/02-typed-mission-graph-todo-write-through-and-handoff-authority/02-01-PLAN.md
@.planning/phases/02-typed-mission-graph-todo-write-through-and-handoff-authority/02-02-PLAN.md
@tools/delegate_tool.py
@tools/checkpoint_manager.py
@agent/skill_commands.py
</context>

<tasks>

<task type="auto">
  <name>task 1: persist typed handoff packets at the delegation seams</name>
  <files>agent/handoff_packets.py, agent/mission_state.py, tools/delegate_tool.py, tests/tools/test_delegate.py, tests/agent/test_handoff_packets.py</files>
  <action>Add a dedicated handoff-packet module and wire `delegate_tool.py` to persist mission-linked packets, child-session provenance, durable review/fix style links, and any metadata needed for later projection-only mission bundles through canonical mission services. Reuse delegated child lineage and restriction data already available in the current pipeline, but do not treat child prompt text or runtime-signal audit rows as the authoritative handoff record.</action>
  <verify>
    <automated>scripts/run_tests.sh tests/tools/test_delegate.py tests/agent/test_handoff_packets.py -q</automated>
  </verify>
  <done>Delegated mission work produces typed canonical handoff records that can be queried without replaying prompt history or audit events.</done>
</task>

<task type="auto">
  <name>task 2: keep mission checkpoints canonical and filesystem checkpoints separate</name>
  <files>agent/mission_state.py, tools/mission_tool.py, tools/checkpoint_manager.py, tests/tools/test_checkpoint_manager.py, tests/agent/test_handoff_packets.py</files>
  <action>Implement mission checkpoint persistence, restore/read APIs, and any bundle/export read models as canonical mission-backed records with projection-only outputs, while keeping `tools/checkpoint_manager.py` strictly responsible for shadow-git filesystem rollback. If terminology or adapter seams need tightening in `checkpoint_manager.py`, make only the changes required to preserve the separation and avoid leaking mission semantics into rollback infrastructure.</action>
  <verify>
    <automated>scripts/run_tests.sh tests/tools/test_checkpoint_manager.py tests/agent/test_handoff_packets.py -q</automated>
  </verify>
  <done>Mission checkpoint behavior is stored and read from canonical mission APIs, and filesystem rollback tests still prove `CheckpointManager` remains a separate safety mechanism.</done>
</task>

<task type="auto">
  <name>task 3: expose reusable mission-control reads through existing Hermes surfaces</name>
  <files>tools/mission_tool.py, agent/skill_commands.py, tests/agent/test_skill_commands.py, tests/agent/test_handoff_packets.py</files>
  <action>Expose the canonical mission, handoff, checkpoint, and projection-only bundle read queries that later CLI/TUI/web surfaces will need, and route any minimal slash/command normalization through `agent/skill_commands.py` rather than inventing a new dispatcher. Keep scope to substrate only: no dashboard UI, no writable projections, and no second runtime path.</action>
  <verify>
    <automated>scripts/run_tests.sh tests/agent/test_skill_commands.py tests/agent/test_handoff_packets.py -q</automated>
  </verify>
  <done>Mission-control read access is available from the existing Hermes command/tool surfaces, and the read path is demonstrably backed by canonical mission state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] scripts/run_tests.sh tests/tools/test_delegate.py tests/tools/test_checkpoint_manager.py tests/agent/test_handoff_packets.py tests/agent/test_skill_commands.py -q
- [ ] Delegation and checkpoint reads resolve against canonical mission/handoff tables, not runtime-signal audit history or shadow-git checkpoint data.
- [ ] Filesystem rollback behavior remains intact and distinct from workflow checkpoint semantics.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Hermes has canonical, queryable handoff, checkpoint, and projection-only bundle authority for later Mission Control surfaces while preserving the existing filesystem rollback safety path.
</success_criteria>

<output>
After completion, create `.planning/phases/02-typed-mission-graph-todo-write-through-and-handoff-authority/02-03-SUMMARY.md`
</output>
