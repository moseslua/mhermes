---
phase: 02-typed-mission-graph-todo-write-through-and-handoff-authority
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - tools/todo_tool.py
  - run_agent.py
  - agent/mission_state.py
  - tests/tools/test_todo_tool.py
  - tests/agent/test_mission_state.py
  - tests/test_hermes_state.py
autonomous: true
requirements:
  - MISSION-02
user_setup: []
must_haves:
  truths:
    - Attached sessions see `todo` mutations reflected in canonical mission task rows rather than a second durable todo store.
    - Unattached sessions keep the current session-local todo experience, including continuity across compression and resume.
    - Attached mission sessions do not derive task truth from old tool echoes or observational audit rows.
  artifacts:
    - tools/todo_tool.py
    - run_agent.py
    - agent/mission_state.py
    - tests/tools/test_todo_tool.py
    - tests/agent/test_mission_state.py
    - tests/test_hermes_state.py
  key_links:
    - tools/todo_tool.py switches between session-local TodoStore behavior and mission-backed write-through via agent/mission_state.py.
    - run_agent.py hydrates and preserves todo continuity correctly across mission attach/detach and compression boundaries.
    - Regression tests prove mission task rows are authoritative when attached and that fallback behavior resumes cleanly when detached.
---

<objective>
Cut the todo system over to canonical mission task rows when a mission is attached, without breaking the existing session-local path when no mission is active.

Purpose: Phase 2 fails if `todo` keeps its own durable truth. This plan converts the existing stable tool interface into an adapter over canonical mission state while preserving the no-mission experience.
Output: Mission-aware todo tool behavior, run-agent hydration/continuity changes, and targeted regressions for write-through and fallback semantics.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-typed-mission-graph-todo-write-through-and-handoff-authority/02-CONTEXT.md
@.planning/phases/02-typed-mission-graph-todo-write-through-and-handoff-authority/02-RESEARCH.md
@.omx/plans/prd-hermes-all-features-integration.md
@.omx/plans/test-spec-hermes-all-features-integration.md
@.planning/phases/02-typed-mission-graph-todo-write-through-and-handoff-authority/02-01-PLAN.md
@tools/todo_tool.py
@run_agent.py
@hermes_state.py
</context>

<tasks>

<task type="auto">
  <name>task 1: convert todo mutations into a mission-backed write-through adapter</name>
  <files>tools/todo_tool.py, agent/mission_state.py, tests/tools/test_todo_tool.py</files>
  <action>Keep the existing `todo` tool schema and user-visible operations intact, but route mutations through `agent/mission_state.py` whenever the current session has an attached mission. Model mission tasks as canonical `mission_nodes` of the approved task type instead of inventing a second task table or reverse-sync bridge. Preserve the current in-memory/session-local path only for sessions with no attached mission.</action>
  <verify>
    <automated>scripts/run_tests.sh tests/tools/test_todo_tool.py -q</automated>
  </verify>
  <done>`todo` add/edit/done/delete can target either the session-local store or mission task rows, with active-mission sessions writing only through canonical mission state.</done>
</task>

<task type="auto">
  <name>task 2: update agent hydration and compression continuity for attach and detach flows</name>
  <files>run_agent.py, tools/todo_tool.py, agent/mission_state.py</files>
  <action>Replace history-scan-as-truth behavior for attached missions with mission-backed hydration in `run_agent.py`, while preserving the existing compression continuity mechanism for active tasks. Detaching a mission must restore the session-local todo view cleanly. Do not let suspended session-local todos leak into the default mission-attached view, and do not infer canonical mission state from old tool responses.</action>
  <verify>
    <automated>scripts/run_tests.sh tests/tools/test_todo_tool.py tests/agent/test_mission_state.py -q</automated>
  </verify>
  <done>Agent startup, rehydration, and compression preserve the correct todo source of truth for both mission-attached and no-mission sessions.</done>
</task>

<task type="auto">
  <name>task 3: prove there is no dual writable todo authority</name>
  <files>tests/tools/test_todo_tool.py, tests/agent/test_mission_state.py, tests/test_hermes_state.py</files>
  <action>Add targeted regressions for the Gate 3 behaviors: attach a mission, mutate todos through the existing tool, confirm mission task rows changed, detach the mission, and confirm session-local todo behavior resumes. Include negative coverage showing runtime-signal audit rows and persisted tool echoes do not become authoritative mission task state.</action>
  <verify>
    <automated>scripts/run_tests.sh tests/tools/test_todo_tool.py tests/agent/test_mission_state.py tests/test_hermes_state.py -q</automated>
  </verify>
  <done>The tests make split-brain behavior observable: a future change that reintroduces a second durable todo truth fails immediately.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] scripts/run_tests.sh tests/tools/test_todo_tool.py tests/agent/test_mission_state.py tests/test_hermes_state.py -q
- [ ] Active-mission todo mutations are asserted against canonical mission rows, not runtime-signal audit output or persisted tool echoes.
- [ ] No-mission sessions still use the existing session-local todo path, including continuity across compression/resume flows.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `todo` becomes a truthful adapter: canonical mission tasks when attached, existing session-local behavior when detached or unattached, with no second durable task authority left behind.
</success_criteria>

<output>
After completion, create `.planning/phases/02-typed-mission-graph-todo-write-through-and-handoff-authority/02-02-SUMMARY.md`
</output>
